<!DOCTYPE html>
<html lang="es">
<head>
    <title>Gestionar Usuarios</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel='stylesheet' href='/stylesheets/admin.css'/> <!-- Puedes crear un archivo CSS específico para la vista del administrador -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <%- include("../header") %>

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="cnt-cuadro">
                    <div class="cnt-admin"><h2>Gestionar Instalaciones</h2></div>
                    
                    <!-- Lista de usuarios -->
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Primer Apellido</th>
                                <th>Segundo Nombre</th>
                                <th>Correo</th>
                                <th>Admin</th>
                                <th>Validado</th>
                                <th>Validar</th>
                                <th>Hacer administrador</th>
                                <th>Expulsar</th>
                            </tr>
                        </thead>
                        <tbody id="user-list">
                            <p></p>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <%- include("../footer") %>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>


    <script>
        $(document).ready(function() {
            // Obtener las instalaciones mediante AJAX
            $.ajax({
                url: '/api/instalaciones',
                method: 'GET',
                success: function(data) {
                    // Generar la tabla con los datos recibidos
                    var table = '<table>';
                    table += '<tr><th>ID</th><th>Nombre</th><th>Acciones</th></tr>';
                    data.forEach(function(instalacion) {
                        table += '<tr>';
                        table += '<td>' + instalacion.id + '</td>';
                        table += '<td>' + instalacion.nombre + '</td>';
                        table += '<td>';
                        table += '<button onclick="editarInstalacion(' + instalacion.id + ')">Editar</button>';
                        table += '<button onclick="eliminarInstalacion(' + instalacion.id + ')">Eliminar</button>';
                        table += '</td>';
                        table += '</tr>';
                    });
                    table += '</table>';
                    $('#instalacionesTable').html(table);
                },
                error: function() {
                    alert('Error al obtener las instalaciones');
                }
            });

            // Función para editar una instalación
            function editarInstalacion(id) {
                // Implementa la lógica para editar una instalación
            }

            // Función para eliminar una instalación
            function eliminarInstalacion(id) {
                // Implementa la lógica para eliminar una instalación
            }
        });
    </script>

</body>
</html>
