<!DOCTYPE html>
<html lang="es">
<head>
    <title>Destinos turísticos</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel='stylesheet' href='/stylesheets/login.css'/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
      <%- include("header") %>
        <div class="cnt-login"><h2>Crea una cuenta</h2></div>
        <form action="/register" method="post" id="formulario">
            <div id="cnt-credencial">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" name="nombre" required value="<%= FormData.nombre || '' %>">
            </div>
            <div id="cnt-credencial">
                <label for="apellido1">Primer Apellido:</label>
                <input type="text" id="apellido1" name="apellido1" required value="<%= FormData.apellido1 || '' %>">
            </div>
            <div id="cnt-credencial">
                <label for="apellido2">Segundo Apellido:</label>
                <input type="text" id="apellido2" name="apellido2" value="<%= FormData.nomapellido2bre || '' %>">
            </div>  
            <div id="cnt-credencial">
                <label for="correo">Correo:</label>
                <input type="text" id="correo" name="correo" required value="<%= FormData.correo || '' %>">
            </div>    
            <div id="cnt-credencial">
                <label for="facultad">Facultad:</label>
                <select class="form-control" id="facultad" name="facultad" required>
                    <option value="Informática" <%= FormData.facultad === 'Informática' ? 'selected' : '' %>>Informática</option>
                    <option value="Biología" <%= FormData.facultad === 'Biología' ? 'selected' : '' %>>Biología</option>
                    <option value="Información" <%= FormData.facultad === 'Información' ? 'selected' : '' %>>Información</option>
                </select>
            </div>
            <div id="cnt-credencial">   
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div id="cnt-credencial">   
                <label for="password">Repite la contraseña:</label>
                <input type="password" id="password2" name="password" required>
            </div>
     
            <button type="submit"  class="btn btn-primary" id="boton">Crear</button>
        </form>

        <!-- Mostrar mensajes de error -->
        <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger" role="alert">
            <ul>
                <% errors.forEach(error => { %>
                    <li><%= error.msg %></li>
                <% }); %>
            </ul>
        </div>
        <% } %>
        <!-- Mostrar mensaje de éxito -->
        <% if (exito) { %>
            <div class="alert alert-success" role="alert">
                ¡Cuenta creada exitosamente!
            </div>
        <% } %>

    <%- include("footer") %>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>
    
   